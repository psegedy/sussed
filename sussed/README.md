# 🕵️‍♂️ sussed (Python project)

This is the Python package for **sussed**. Run all commands from this directory:

```bash
uv sync
uv run sussed --help
```

See the [main README](../README.md) for the overview and quick start, plus:

- **[CLI reference](docs/cli.md)** — every command and flag
- **[Configuration reference](docs/configuration.md)** — env vars + hunt-config YAML schema
